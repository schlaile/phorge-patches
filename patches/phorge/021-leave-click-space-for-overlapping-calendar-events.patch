diff --git a/webroot/rsrc/js/application/calendar/behavior-day-view.js b/webroot/rsrc/js/application/calendar/behavior-day-view.js
index 24d592ddcc..78b2eab751 100644
--- a/webroot/rsrc/js/application/calendar/behavior-day-view.js
+++ b/webroot/rsrc/js/application/calendar/behavior-day-view.js
@@ -10,6 +10,8 @@
  */
 JX.behavior('day-view', function(config) {
 
+  var tail_gap = 16;
+
   function findTodayClusters() {
     var events = today_events.sort(function(x, y){
       return (x.eventStartEpoch - y.eventStartEpoch);
@@ -66,6 +68,14 @@ JX.behavior('day-view', function(config) {
       var event_id = cluster_member.eventID;
       var offset = ((n / cluster_size) * 100) + '%';
       var width = ((1 / cluster_size) * 100) + '%';
+      var is_cluster_tail = (n === (cluster_size - 1));
+
+      // Leave a small amount of click-through space to the right of the last
+      // event in a cluster so users can still create overlapping events
+      // directly from the day view.
+      if (is_cluster_tail) {
+        width = 'calc(' + width + ' - ' + tail_gap + 'px)';
+      }
 
       for (var j=0; j < today_events.length; j++) {
         if (today_events[j].eventID == event_id) {
diff --git a/resources/celerity/map.php b/resources/celerity/map.php
index ad620af6f3..b6ac35b823 100644
--- a/resources/celerity/map.php
+++ b/resources/celerity/map.php
@@ -295,7 +295,7 @@ return array(
     'rsrc/js/application/aphlict/behavior-aphlict-listen.js' => 'a4859af5',
     'rsrc/js/application/aphlict/behavior-aphlict-status.js' => 'da402bda',
     'rsrc/js/application/aphlict/behavior-desktop-notifications-control.js' => '9e3e8389',
-    'rsrc/js/application/calendar/behavior-day-view.js' => '1a6b7115',
+    'rsrc/js/application/calendar/behavior-day-view.js' => 'ff8ee473',
     'rsrc/js/application/calendar/behavior-event-all-day.js' => 'a64672b5',
     'rsrc/js/application/calendar/behavior-month-view.js' => 'e5e044e8',
     'rsrc/js/application/config/behavior-reorder-fields.js' => '2539f834',
@@ -543,7 +543,7 @@ return array(
     'javelin-behavior-dashboard-move-panels' => 'a2ab19be',
     'javelin-behavior-dashboard-query-panel-select' => '1e413dc9',
     'javelin-behavior-dashboard-tab-panel' => '0116d3e8',
-    'javelin-behavior-day-view' => '1a6b7115',
+    'javelin-behavior-day-view' => 'ff8ee473',
     'javelin-behavior-desktop-notifications-control' => '9e3e8389',
     'javelin-behavior-detect-timezone' => '4321459b',
     'javelin-behavior-device' => 'f941cb00',
@@ -1019,15 +1019,6 @@ return array(
       'javelin-install',
       'javelin-util',
     ),
-    '1a6b7115' => array(
-      'javelin-behavior',
-      'javelin-dom',
-      'javelin-stratcom',
-      'javelin-uri',
-      'javelin-vector',
-      'javelin-workflow',
-      'phuix-icon-view',
-    ),
     '1b6acc2a' => array(
       'javelin-magical-init',
       'javelin-util',
@@ -2332,6 +2323,15 @@ return array(
       'owners-path-editor',
       'javelin-behavior',
     ),
+    'ff8ee473' => array(
+      'javelin-behavior',
+      'javelin-dom',
+      'javelin-stratcom',
+      'javelin-uri',
+      'javelin-vector',
+      'javelin-workflow',
+      'phuix-icon-view',
+    ),
   ),
   'packages' => array(
     'conpherence.pkg.css' => array(
